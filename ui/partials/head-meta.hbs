<link rel="icon" href="{{uiRootPath}}/img/favicon.png" type="image/png">
<style type="text/css">
  .algolia-autocomplete, #algolia-search {
    width: 100%!important;
    vertical-align: middle;
    height: 2rem;
    border-color: #1565c0;
  }
  .nav-panel-menu {
    overflow: visible;
  }
  code.language-shell .hljs-meta {
    user-select: none;
  }
  .icon .fa.fa-external-link {
    font-size: .6rem;
    vertical-align: super;
  }
  .nav-panel-menu {
    overflow-y: auto;
    height: calc(100vh - 3.5rem - 2.5rem);
    min-height: calc(100vh - 3.5rem - 2.5rem) !important;
    max-height: calc(100vh - 3.5rem - 2.5rem);
    padding-bottom: 2.5rem;
  }
  div.nav-panel-explore span.version {
    min-width: 7em;
  }
  /* Link color wins over code color */
  a code {
    color: inherit !important;
  }
  /* Highlight URL fragment focus */
  :target a.link {
    background-color: yellow;
  }
  /* Redundant component+version selection widget, redundant because of global menu. */
  .nav-panel-explore {
    display: none;
  }
</style>
<link
  rel="stylesheet"
  href="https://cdn.jsdelivr.net/npm/docsearch.js@2/dist/cdn/docsearch.min.css"
  />
<link
  rel="stylesheet"
  href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css"
  />
